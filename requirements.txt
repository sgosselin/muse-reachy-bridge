fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
cryptography>=42
numpy>=1.26
pillow>=10
# Only needed for full-size Reachy 1/2/2023 (classic gRPC SDK):
# reachy-sdk
# Camera/mic/speaker on a real Reachy Mini need the Mini SDK on the
# bridge host (imported lazily; media endpoints 501 without it):
# reachy-mini
